version: '2'

options:
  verbose: false

syncs:
  php-syncs:
    src: '../mycode/'
    sync_strategy: 'native_osx'
    sync_host_ip: '127.0.0.1'
    sync_args: ['-perms=0']
    sync_excludes: ['.idea', '.git','.DS_Store']
